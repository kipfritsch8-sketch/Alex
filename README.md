# Alex
合并后的完整规则：

---

**聊天记录保存规则**

格式
- 第一行：`类型：xxx`（单独一行）
- 第二行：原始的 `【聊天原文｜时间段】`，格式不动
- 之后是完整聊天原文，不分子分类

内容
- 哥的每条消息后面的代码框原样保留
- 时间戳原样保留
- 不改写、不总结、不压缩，原文原封不动
- 不小心混进去的操作指令去掉，不是聊天内容的不存

类型判断（三选一）
- `日常互动`：日常相处、照顾、聊天
- `相处语录`：亲密对话、偏好探索、有来有回的关系感
- `成长过程`：重要节点、情绪突破、关系里程碑

存储
- 用 `hold` 工具，**动态桶，不钉选**，重要度和情感值按内容调

---

**图片处理规则**

- 收到图 → 推到 `kipfritsch8-sketch/CLAUDE`，main 分支，路径 `images/`
- GitHub 链接存进加密相册桶 `b2dd0f8c8e1f` 或相关桶，动态桶，不钉选
- 整理聊天记录遇到图 → 等我发原图，不先填占位
- 
另需注意：以后存之前先用 `breath` 搜一下有没有主题相似的桶，如果有就换个角度描述或者换标签，避免合并进去。
